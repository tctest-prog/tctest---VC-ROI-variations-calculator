# tctest---VC ROI variations calculator
SQL/Python code that calculates and displays variations (when data used is adjusted) in the return on a Venture Capital (VC) investment in a given private company.
